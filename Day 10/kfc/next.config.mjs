/** @format */

const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "static.order.kfcku.com",
        port: "",
      },
    ],
  },
};

export default nextConfig;
